matplotlib>=3.8
python-dotenv>=1.0
# Browser TLS impersonation for endpoints behind Cloudflare Browser Integrity
# Check (e.g. Ramp Router api.router.com). Optional: only required when a
# model config sets backend_kwargs.impersonate.
curl-cffi>=0.9

# --- Local vLLM serving (install separately; not pinned to torch/CUDA matrix) ---
#   uv pip install vllm
#
# Do NOT install flash-attn for the vLLM experiment path.
# vLLM ships its own efficient attention kernels (FlashAttention-class + PagedAttention).
# Compiling flash-attn often needs >>16–30GB RAM and is unnecessary for
# configs/*_vllm.json.
