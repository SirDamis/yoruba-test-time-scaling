torch>=2.3
transformers>=4.45,<5
accelerate>=0.33
datasets>=2.20
sentencepiece>=0.2
protobuf>=4.25
safetensors>=0.4
matplotlib>=3.8

# --- Local vLLM serving (install separately; not pinned to torch/CUDA matrix) ---
#   uv pip install vllm
#
# Do NOT install flash-attn for the vLLM experiment path.
# vLLM ships its own efficient attention kernels (FlashAttention-class + PagedAttention).
# Compiling flash-attn often needs >>16–30GB RAM and is unnecessary for
# configs/*_vllm.json.
#
# Transformers-only runs (configs/e1_reasoning_language.json etc.) use
# attn_implementation=auto → SDPA by default; FA2 is optional and not required.
