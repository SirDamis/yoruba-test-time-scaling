# Discriminative-PRM extras (install on top of the repo requirements.txt).
# Run these on the cloud GPU box, not the local CPU machine:
#   uv pip install -r prm/requirements-prm.txt
#
# torch/transformers/accelerate/datasets are only needed for train + score.

torch
transformers>=4.45
datasets
huggingface_hub            # download released multilingual PRM800K
accelerate
peft>=0.13
scikit-learn               # AUC / log-loss / accuracy during PRM eval

# Only needed for QLoRA (``load_in_4bit: true`` in the configs).
bitsandbytes>=0.44
