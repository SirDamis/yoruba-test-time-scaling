[project]
name = "yoruba-ttcs"
version = "0.1.0"
description = "Dataset utilities for Yoruba test-time compute scaling experiments."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.uv]
package = false
